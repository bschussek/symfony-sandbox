<?xml version="1.0" ?>

<container xmlns="http://www.symfony-project.org/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.symfony-project.org/schema/dic/services http://www.symfony-project.org/schema/dic/services/services-1.0.xsd">

    <parameters>
        <parameter key="liip.validator.unique.class">Liip\BlogBundle\Constraints\UniqueValidator</parameter>
    </parameters>

    <services>
        <service id="liip.validator.unique" class="%liip.validator.unique.class%">
            <tag name="validator.constraint_validator" alias="liip.validator.unique" />
            <argument type="service" id="doctrine.orm.default_entity_manager" />
        </service>
    </services>
</container>
